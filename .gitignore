# ─── Python ──────────────────────────────────────────────────────────
__pycache__/
*.py[codz]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
MANIFEST

# Test / coverage
.coverage
.coverage.*
.cache
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
htmlcov/

# Type checkers
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# Linters / formatters
.ruff_cache/

# Env
.env
.envrc
.venv
env/
venv/
ENV/

# Editors
.idea/
# .vscode/   # uncomment if you don't want to share workspace settings

# ─── Go ──────────────────────────────────────────────────────────────
# Compiled sidecar binaries (built per-platform during wheel build)
/bin/
*.exe
*.exe~

# Go test/coverage
*.test
*.out

# ─── FastMeow runtime artifacts (NEVER commit) ───────────────────────
# WhatsApp session storage (contains paired device credentials)
sessions/
e2e-smoke-sessions/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
manifest.json
.fastmeow/

# Bundled sidecar binary location inside the wheel build tree
src/fastmeow/_bin/

# ─── Reference clones (kept locally, not committed) ──────────────────
/whatsmeow/

# ─── Tooling metadata (OhMyOpenCode / Sisyphus) ──────────────────────
.sisyphus/

# ─── PyPI / packaging ────────────────────────────────────────────────
.pypirc

# ─── Misc ────────────────────────────────────────────────────────────
*.log
*.pid
